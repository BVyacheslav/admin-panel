// import { createSelector } from 'reselect'

export const getLaptops = ({ laptops }) => laptops;
// export const getLaptops = createSelector(
//   laptopsSelector,
//   laptops => laptops.filter(item => item.id === 1)
// );
